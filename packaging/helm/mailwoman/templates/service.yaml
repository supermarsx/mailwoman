apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailwoman.fullname" . }}
  labels:
    {{- include "mailwoman.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "mailwoman.selectorLabels" . | nindent 4 }}
