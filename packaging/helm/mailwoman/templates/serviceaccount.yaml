apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mailwoman.serviceAccountName" . }}
  labels:
    {{- include "mailwoman.labels" . | nindent 4 }}
# The app never talks to the Kubernetes API; do not mount a token.
automountServiceAccountToken: false
