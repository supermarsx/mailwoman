# Mailwoman — tasks module strings (source locale: en).
# Lazily loaded by the tasks area (loadCatalog('tasks')). User-controlled task
# titles are isolated / <bdi>-wrapped at the call site.

tasks-title = Tasks
tasks-lists = Task lists
tasks-all = All tasks
tasks-my-day = My Day
tasks-lists-heading = Lists
tasks-no-lists = No lists yet.
tasks-new-title = New task title
tasks-add-placeholder = Add a task…
tasks-my-day-empty = Nothing due today. Enjoy the quiet.
tasks-convert = Convert:
tasks-mail-id = Message id to convert
tasks-mail-id-placeholder = message id
tasks-mail-to-task = Mail → task
tasks-event-id = Event id to convert
tasks-event-id-placeholder = event id
tasks-event-to-task = Event → task
tasks-followup-mail = Follow up: mail { $id }
tasks-prep-event = Prep: event { $id }
tasks-list-label = Tasks
tasks-subtasks = Subtasks
tasks-empty-list = No tasks in this list.
tasks-complete = Complete { $title }
tasks-reopen = Reopen { $title }
tasks-high-priority = High priority
