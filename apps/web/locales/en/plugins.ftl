# Mailwoman — UI-plugin host surface strings (source locale: en).
#
# Copy for the SolidJS UI-plugin tier (src/plugins-ui/Tier.tsx) — the host-rendered
# chrome around sandboxed plugin frames. Lazily loaded via `loadCatalog('plugins')`
# when the tier mounts. Message ids are kebab-case and MODULE-PREFIXED (plugins-*).

# -- Unsigned-plugin trust banner --------------------------------------------
plugins-unsigned-warning-label = Unsigned UI plugin warning
plugins-unsigned-title = Unsigned plugins are running
