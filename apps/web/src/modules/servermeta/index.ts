// Public barrel for the web server/mailbox METADATA module (t13 e8). E9 mounts
// the view and injects a production `AclClient` + edit permission.

export { MetadataView, default } from './MetadataView.tsx';
export type { MetadataViewProps } from './MetadataView.tsx';
