{
  "name": "@mailwoman/desktop-e2e",
  "version": "26.6.0",
  "private": true,
  "type": "module",
  "description": "Live E2E for the Mailwoman desktop shell (Tauri v2) via tauri-driver + WebdriverIO. Launches the REAL built app, drives the SPA + the native mw_* capability commands through WebView2 (Windows) / WebKitWebDriver (Linux).",
  "scripts": {
    "test": "wdio run ./wdio.conf.mjs"
  },
  "devDependencies": {
    "@wdio/cli": "^9.20.0",
    "@wdio/local-runner": "^9.20.0",
    "@wdio/mocha-framework": "^9.20.0",
    "@wdio/spec-reporter": "^9.20.0"
  }
}
