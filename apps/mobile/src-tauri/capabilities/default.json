{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the Mailwoman mobile shell's main window. e2/e4 extend this as they add UnifiedPush / share / FLAG_SECURE surfaces; least-privilege (SPEC §7).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "os:default",
    "notification:default",
    "deep-link:default"
  ]
}
