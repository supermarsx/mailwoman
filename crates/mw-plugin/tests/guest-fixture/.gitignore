# Build artifacts of the standalone (own-workspace) guest fixture crate. The
# COMPONENT it produces is committed pre-built at ../fixtures/guest.wasm; the
# intermediate target/ tree must never be committed.
/target
