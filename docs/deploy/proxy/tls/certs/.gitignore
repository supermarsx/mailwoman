# Generated by ../gen-certs.sh. Never commit TLS key material, not even a
# throwaway self-signed test key. This directory is tracked (so the compose
# read-only mounts have something to bind) but its contents are not.
*
!.gitignore
